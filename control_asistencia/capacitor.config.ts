import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'control_asistencia',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
